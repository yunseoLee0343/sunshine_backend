# Sunshine Frontend — MVP Release Report

**Generated**: 2026-05-11 16:43:10  
**Ticket**: TICKET-042  
**Overall**: `PASS`

---

## Automated Gate Results

| Check | Result |
|-------|--------|
| TypeScript (`tsc --noEmit`) | `PASS` |
| ESLint (`npm run lint`) | `PASS` |
| Production build (`npm run build`) | `PASS` |
| API smoke test (backend health + /home) | `SKIP` |

---

## Manual Smoke Checklist

Complete the following 12-step golden path with the backend running and demo data seeded.  
Mark each item `[x]` when confirmed, `[-]` if skipped, `[!]` if failed.

### Onboarding (T-037)

- [ ] **Step 1** — Image picker shows 4 mock plant images and selecting one enables "종 분석하기"
- [ ] **Step 2** — Species candidates list renders with confidence badges; first candidate is pre-selected
- [ ] **Step 3** — Nickname input validates (empty → error, >20 chars → error); room chips selectable
- [ ] **Step 4** — Success card displays plant nickname, species name, room, and character message

### Home & Plant Detail (T-038)

- [ ] **Step 5** — Home screen lists plant cards with mood avatar, care status pill, and today-action banner
- [ ] **Step 6** — Plant detail shows character emoji, 4-sensor grid (soil/temp/humidity/lux), 24h and 7d summary panels

### Care Log (T-039)

- [ ] **Step 7** — "물 주었어요" button sends request and shows character feedback card on success
- [ ] **Step 8** — Note form blocks empty submission; successful note appears in the log timeline immediately

### Chat (T-040)

- [ ] **Step 9** — Chat renders 4-section answer (결론 / 근거 / 행동 / 주의) without layout breaks
- [ ] **Step 10** — Pest-related question ("병충해가 생긴 것 같아") triggers red PestCautionBanner
- [ ] **Step 11** — Preset bar buttons submit questions and scroll the answer into view

### Growth History (T-041)

- [ ] **Step 12** — History timeline shows events newest-first; FilterTabs correctly narrow to 관리/환경/상태

---

## Known Limitations

- `/plants/{plant_id}/history` endpoint not yet implemented in backend; history page falls back to care-logs.
- No unit test suite exists for frontend components (Vitest not configured for T-036–T-041 scope).
- Smoke test step 4c (`GET /home`) only verifies HTTP 200, not response schema.

---

## Sign-off

| Role | Name | Date | Signature |
|------|------|------|-----------|
| Frontend Dev | | | |
| QA Reviewer | | | |
| Release Manager | | | |

---

*Generated by `scripts/frontend_release_gate.sh` — Sunshine Backend MVP*
